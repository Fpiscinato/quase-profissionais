/**
 * Manually bumped on user-visible changes — simpler to read at a glance than
 * a git hash. Format: major.minor, minor counts 1-9 then rolls to the next
 * major (e.g. 1.9 -> 2.0). Bump this whenever a batch of changes ships.
 */
export const APP_VERSION = '1.5'
